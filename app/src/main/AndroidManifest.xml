<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.ribosoft.personalfinance">
    <!-- thanks to my mother and to those who allowed me to make my dreams come true -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalFinance">
        <activity android:name=".GestisciMovimentoActivity"></activity>
        <activity
            android:name=".NuovoMovimentoActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MovimentiBancaActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NuovaBancaActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>